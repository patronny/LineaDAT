import type { Metadata } from "next";

export const metadata: Metadata = { title: "Yield DATs (coming soon)" };

export default function YieldDatsDocPage() {
  return (
    <>
      <h1>Yield DATs (coming soon)</h1>
    </>
  );
}
